
console.pretty = function(i) {
  // for now, log
  console.log(i);
  
  // get type of input
  var inputType = typeof i;
  
  // different action dependng on type
    
  switch(inputType) {
    case string :
        break;
    case array :
        break;
    case object :
        if ( not null ) {
            if ( one-level deep object ) {
            }
            else {
            }
        }
        else {
        }
        break;        
    case boolean :
        break;     
    case undefined :
        break;
    default :
  }

}
